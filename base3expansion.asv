

x=86;
runs = int64(log(86)/log(3))+1;
coordinates = zeros(runs,1);

for r =1:runs
    ex = (3^(runs-r));
    if(ex>result)
        coordinates(r) = 1;
    else
    result = (x/ex);
    coordinates(r) = +1
    x = x - (ex*(coordinates(r)-1));
    end
end
